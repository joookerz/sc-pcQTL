# Example dataset

This directory is reserved for a redistributable biological example dataset,
its parameter file, and expected checksums. The dataset will be added before
the first stable release.

The synthetic files in `tests/fixtures/` already exercise the core workflow
in continuous integration. They are deliberately small software fixtures and
must not be used for biological interpretation or performance benchmarking.
